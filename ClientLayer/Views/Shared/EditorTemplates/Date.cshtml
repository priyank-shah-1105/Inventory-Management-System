@using Kendo.Mvc.UI
@model DateTime?

@(Html.Kendo().DatePickerFor(m => m).Min(DateTime.Now)
      .HtmlAttributes(new { id = ViewData.TemplateInfo.GetFullHtmlFieldName(string.Empty) + "_wrapper", style = "display:block !important" })
      )