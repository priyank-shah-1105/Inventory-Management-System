@using CommonLayer
@using Kendo.Mvc.UI
@model object

@(
 Html.Kendo().DropDownListFor(m => m)
 .OptionLabel(CommonHelper.PleaseSelect)
        .BindTo((SelectList)ViewData[ViewData.TemplateInfo.GetFullHtmlFieldName("") + "_Data"])
)
